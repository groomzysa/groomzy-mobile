mutation updateProvider(
  $providerId: Int!
  $tradingName: String
  $phone: String
  $logo: File
) {
  updateProvider(
    providerId: $providerId
    tradingName: $tradingName
    phone: $phone
    logo: $logo
  ) {
    id
    tradingName
    phone
    logoUrl
  }
}
